{
  "name": "bus-navigator-relay",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket relay that keeps the Sarvam key server-side for realtime streaming",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.90.0"
  }
}
